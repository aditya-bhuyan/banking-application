# Server Configuration
server.port=8080
server.servlet.context-path=/banking

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://aditya:aditya@cluster0.80ubj.mongodb.net/banking_db?retryWrites=true&w=majority
spring.data.mongodb.database=banking_db

# Spring Data MongoDB Settings (Optional, but useful for tuning performance)
spring.data.mongodb.authentication-database=admin
spring.data.mongodb.username=aditya
spring.data.mongodb.password=aditya

# Enable Spring Boot DevTools for hot reload (optional)
spring.devtools.restart.enabled=true

# Enable JPA / MongoDB logging
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.springframework.data.mongodb.core.query=DEBUG

# Enable Spring Security (optional, if you plan to implement authentication)
#spring.security.user.name=admin
#spring.security.user.password=password

# Thymeleaf Configuration (Optional, if you use templates)
spring.thymeleaf.cache=false

# Logging Configuration (Optional)
logging.level.org.springframework=INFO
logging.level.com.bank=DEBUG

# Profiles Configuration (e.g., for different environments)
#spring.profiles.active=dev

# Application name
spring.application.name=banking-application

# Error handling settings (Optional)
server.error.whitelabel.enabled=false

# Spring Boot Banner (Optional, you can disable the banner in the terminal)
spring.main.banner-mode=off
